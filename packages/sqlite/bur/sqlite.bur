#|

low level interface to sqlite

|#

data Value:
  | value(v)
end

data Handle:
  | handle(h)
end

open :: String -> Handle
fun open(a): ... end

create = ...

close = ...

command = ...

data ResultHandle:
  | ResultHandle(rh)
end

nextRow = ...

closeResult = ...

query = ...
