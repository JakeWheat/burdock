
# test the ffi example

include filesystem
import from lists:
  map,
  filter,
  not-elem
end

include testing

fun my-ld(dir):
  glob(dir + "**/*.bur") + glob(dir + "**/*.rst")
end

srcs = my-ld("src/packages/ffitypes-test")

default-test-run-exe([list: {(x): x.{test-sources:map(file-test(_), srcs)}}])
