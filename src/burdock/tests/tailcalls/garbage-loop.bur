my-sz = 64 * 1000 * 1000
iters = 10

fun do-it(sz):
  bs = make-bytestring(sz)
  get-bytestring-byte(bs, 10)
end

fun garbage-loop(n,sz):
  do-it(sz)
  shadow n = n - 1
  when n > 0: garbage-loop(n,sz) end
end

garbage-loop(iters, my-sz)
