my-sz = 64 * 1000 * 1000
iters = 10

fun non-tail-call-loop(n,sz):
  bs = make-bytestring(sz)
  shadow n = n - 1
  when n > 0:
    non-tail-call-loop(n,sz)
     #print(1)
  end
  get-bytestring-byte(bs, 10)
end

non-tail-call-loop(iters, my-sz)
